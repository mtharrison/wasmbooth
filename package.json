{
  "name": "wasm-booth",
  "version": "1.0.0",
  "description": "",
  "main": "lib/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cargo +nightly build --target wasm32-unknown-unknown && mv target/wasm32-unknown-unknown/debug/wasm_video_filters.wasm public",
    "build-release": "cargo +nightly build --release --target wasm32-unknown-unknown && mv target/wasm32-unknown-unknown/release/wasm_video_filters.wasm public"
  },
  "keywords": [],
  "author": "Matt Harrison<hi@matt-harrison.com>",
  "license": "MIT",
  "dependencies": {
    "hapi": "^17.3.1",
    "inert": "^5.1.0"
  },
  "devDependencies": {
    "webpack": "^4.6.0",
    "webpack-cli": "^2.0.15"
  }
}